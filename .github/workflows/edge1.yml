name: edge1

on:
  push:
    branches: [ "main" ]
    paths:
      - edge1/**
  pull_request:
    branches: [ "main" ]
    paths:
      - edge1/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build
        run: ./edge1/build.sh 
